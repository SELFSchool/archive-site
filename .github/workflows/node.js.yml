name: scrape
on: 
   workflow_dispatch:  
   schedule:
    - cron:  '6 6 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Dependencies
        run: npm ci
      - name: run main flow
        run: ./scrape.sh

